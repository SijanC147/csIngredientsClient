version: "1.0"
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm link @angular/cli
    build:
      commands:
        - ng build
  artifacts:
    baseDirectory: dist
    files: "**/*"
