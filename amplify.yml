version: "1.0"
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run-script build -- --prod
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
