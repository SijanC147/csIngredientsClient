import { NutrientAmountPipe } from './nutrient-amount.pipe';

describe('NutrientAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new NutrientAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
